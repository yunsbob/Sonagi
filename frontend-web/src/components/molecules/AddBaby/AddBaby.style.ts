import styled, { css } from 'styled-components';

const AddBabyContainer = styled.div`
  display: flex;
  align-items: center;
  gap: 0.5rem;
  &:hover {
    cursor: pointer;
  }
`;

export default AddBabyContainer;
