import styled from 'styled-components';

const RecordBarContainer = styled.div`
  display: flex;
  overflow-x: scroll;
  gap: 4px;
  position: fixed;
  bottom: 62px;
  margin-left: 0.5rem;
`;

export { RecordBarContainer };
