import styled from 'styled-components';

export const RecordContainerStyle = styled.div`
  margin: 0 0.75rem 0.5rem 0.75rem;
  overflow-y: auto;
  padding-top: env(safe-area-inset-top);
  height: calc(100vh - env(safe-area-inset-top));
  overflow-y: auto;
`;
