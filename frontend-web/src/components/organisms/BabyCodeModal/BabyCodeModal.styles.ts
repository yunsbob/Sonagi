import { styled } from 'styled-components';

const BabyCodeModalContainer = styled.div``;

const BabyCodeWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;

  p {
    margin-right: 20px;
  }
`;

export { BabyCodeModalContainer, BabyCodeWrapper };
